if, elif, and else are conditional statements used for decision-making in Python.
if checks the first condition.
elif checks multiple alternative conditions.
else executes when none of the conditions are true.
Conditions are evaluated from top to bottom
Only one block executes when a condition is satisfied# If--Else--Elif
